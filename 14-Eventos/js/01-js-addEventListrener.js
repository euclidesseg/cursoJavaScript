document.addEventListener(`DOMContentLoaded`, () => {
    //Este va a ser uno de los eventos que mas se utilizara
    // Este es un evento que se ejecuta una vez que es descargado todo el codigo html es decir, 
    // hasta que todo el documento no este listo imagenes y demas no se va a ejecutar
    console.log(`Documento listo`);
}) 